import styled from 'styled-components';
import { Button } from '@mui/material';

export const CustomCharactersButton = styled(Button)`
  && {
    border-radius: 16px;
    text-transform: initial;
  }
`;
